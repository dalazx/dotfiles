[user]
	name = Oleksandr Danshyn
	email = dalazx@gmail.com
[color]
	ui = true
	diff = always
	status = always
[push]
	default = simple

[merge]
	tool = vimdiff
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
